import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
    base: '/docent-sample/', // Set to the specific repository name for GitHub Pages to resolve assets correctly
})
